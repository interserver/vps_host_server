OpenLiteSpeed WordPress is ready.

Site URL:    http://{$ip}/  (https:// once your domain DNS is pointed and TLS is issued)
Admin panel: http://{$ip}/wp-admin/

WordPress admin login:
  Username: siteadmin
  Password: {$password}
  Email:    {$email}

OpenLiteSpeed WebAdmin console:
  URL:      https://{$ip}:7080/
  Username: admin
  (WebAdmin password is in /root/openlitespeedwordpress-credentials.txt)

Server (SSH):
  ssh root@{$ip}
  Root password: {$password}

Next steps:
  1. Point your domain's DNS A record at {$ip}.
  2. Log in at /wp-admin/ and configure your site.
  3. Full generated credentials (DB, MySQL root, OLS WebAdmin) are in
     /root/openlitespeedwordpress-credentials.txt on the server.
