Your LEMP stack (Nginx + MariaDB + PHP-FPM) server is ready.

Web:
  http://{$ip}/        (HTTPS once a domain is pointed and TLS is issued)
  Web root: /var/www/lemp

There is no web control panel. The application database password is:
  DB password: {$password}
Admin / Let's Encrypt email: {$email}

Server (root) access:
  ssh root@{$ip}     (password: {$password})

Next steps:
  1. Upload your PHP application into /var/www/lemp.
  2. Connect your app to MariaDB (database lemp_app, user lemp_user,
     password {$password}).
  3. Point a domain at {$ip}, set APP_DOMAIN + LE_EMAIL in
     /root/.lemp-bootstrap.env, and re-run certbot for HTTPS.
  4. Full details (including MySQL root password) are in
     /root/lemp-credentials.txt
