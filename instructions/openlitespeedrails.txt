Your OpenLiteSpeed + Ruby on Rails server is ready.

Rails app:
  http://{$ip}/        (HTTPS once a domain is pointed and TLS is issued)

OpenLiteSpeed WebAdmin console (from the server / SSH tunnel):
  https://127.0.0.1:7080
  Admin user: admin
  Password:   {$password}
Admin / Let's Encrypt email: {$email}

To reach the WebAdmin from your machine, tunnel it over SSH:
  ssh -L 7080:127.0.0.1:7080 root@{$ip}
then open https://localhost:7080

Server (root) access:
  ssh root@{$ip}     (password: {$password})

Next steps:
  1. Deploy your Rails app into the app directory; PostgreSQL is preconfigured.
  2. Point a domain at {$ip}, set APP_DOMAIN + LE_EMAIL in
     /root/.openlitespeedrails-bootstrap.env, and re-run certbot for HTTPS.
  3. Full details (DB + Rails SECRET_KEY_BASE) are in
     /root/openlitespeedrails-credentials.txt
