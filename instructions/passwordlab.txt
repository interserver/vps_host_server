PasswordLab (Vaultwarden, a Bitwarden-compatible password manager) is installed
on your VPS.

Web vault: http://your-domain/  (https once TLS is issued)
Admin panel: http://your-domain/admin
Admin token: {$password}
Let's Encrypt email: {$email}

SSH access:  ssh root@{$ip}   (password: {$password})

NEXT STEPS: point your domain's A record to {$ip}. If TLS was deferred, run:
  certbot --nginx -d your-domain
Open the web vault and create your first account, then visit /admin (using the
token above) to configure SMTP and disable public signups. Connect any official
Bitwarden client app to http(s)://your-domain/.
Full credentials: /root/passwordlab-credentials.txt
