Your Forgejo (self-hosted Git) server is ready.

Web UI:
  http://{$ip}/        (HTTPS once a domain is pointed and TLS is issued)

Log in with:
  Admin user:  forgejo_admin
  Password:    {$password}
  Admin email: {$email}

Git over SSH uses port 2222, e.g.:
  git clone ssh://git@{$ip}:2222/user/repo.git

Server (root) access:
  ssh root@{$ip}     (password: {$password})

Next steps:
  1. Open the web UI and log in as forgejo_admin.
  2. Point your domain's A record at {$ip}, set APP_DOMAIN + LE_EMAIL in
     /root/.forgejo-bootstrap.env, and re-run certbot for HTTPS.
  3. Create your first repository or organization.
  4. Full details are in /root/forgejo-credentials.txt
