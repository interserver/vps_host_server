Your WooCommerce (WordPress) store is ready.

Storefront:
  http://{$ip}/                 (HTTPS once a domain is pointed and TLS is issued)
WordPress admin:
  http://{$ip}/wp-admin/
WooCommerce admin:
  http://{$ip}/wp-admin/admin.php?page=wc-admin

Log in with:
  Admin user:  storeadmin
  Password:    {$password}
  Admin email: {$email}

Server (root) access:
  ssh root@{$ip}     (password: {$password})

Next steps:
  1. Log in at http://{$ip}/wp-admin/ as storeadmin.
  2. Point your domain's A record at {$ip}, set APP_DOMAIN + LE_EMAIL in
     /root/.woocommerce-bootstrap.env, and re-run certbot for HTTPS.
  3. Complete the WooCommerce store setup wizard (currency, products, payments).
  4. Full details (DB + MySQL root password) are in
     /root/woocommerce-credentials.txt
